services:
  dynamodb-local:
    command: "-jar DynamoDBLocal.jar -inMemory"
    image: "amazon/dynamodb-local:latest"
    container_name: nexspec-dynamodb-local
    ports:
      - "8000:8000"
    working_dir: /home/dynamodblocal
